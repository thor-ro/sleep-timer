<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runtime-requested by MainActivity so the countdown notification can be shown. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- A user-set timer is exactly the category Google allows this permission for; still
         falls back to an inexact alarm if canScheduleExactAlarms() ever returns false. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SleepTimer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SleepTimer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Bound by the system as soon as the user enables it in Accessibility settings; its
             only job is to call performGlobalAction(GLOBAL_ACTION_LOCK_SCREEN) once the timer
             expires. It also receives ACTION_START/ACTION_ABORT/ACTION_ALARM_FIRED intents. -->
        <service
            android:name=".TimerService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/app_name"
            android:exported="true">

            <intent-filter>
                <!-- Dies sorgt dafür, dass er in den Bedienungshilfen erscheint -->
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <!-- Verweist auf die Konfigurations-XML in res/xml/ -->
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
